// @ts-check

import { defineConfig } from 'astro/config';

export default defineConfig({
  // 替换成你的 GitHub 用户名和仓库名
  site: 'https://20040120lxs.github.io',
  base: '/my_blog', // 如果是部署到主域名（username.github.io），则不需要这行
});